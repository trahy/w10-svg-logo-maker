// creating a parent class which will extend to the children class
class Shape {
    constructor(shapeColour, text, textColour) {
        this.shapeColor = shapeColour;
        this.text = text;
        this.textColor = textColour;
    }
};

